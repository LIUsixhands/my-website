# 複製成 .env 再填（.env 不會進 git）
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash
# XIAOKE_PORT=8788

# 有自己的固定網址（Cloudflare 具名通道／自己的網域）才填；留空就用 cloudflared 快速通道
# PUBLIC_URL=https://line.example.com
# cloudflared 不在 PATH 時才填
# CLOUDFLARED_PATH=C:\Program Files (x86)\cloudflared\cloudflared.exe
