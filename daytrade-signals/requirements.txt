# 盤前／盤中／盤後三支程式需要的相依。
# 離線測試（test_daytrade.py）與 dryrun.py 只用標準庫，不需要安裝任何東西。

# 目前穩定版為 1.7.x（官方文件標示 v1.7.5，支援 Python 3.7~3.14）。
# 上限鎖在 2.0：本專案是照 1.x 的 API 寫的，將來 v2 破壞性改版
# 不該被無聲裝上去 —— 那會在盤中才炸。
shioaji>=1.7,<2
requests>=2.31
